{
    "name": "controlabs/http-exceptions",
    "type": "library",
    "description": "PHP HTTP Status Code Exceptions",
    "keywords": ["PHP", "http", "exceptions"],
    "homepage": "https://github.com/controlabs/php-http-exceptions",
    "license": [
        "MIT"
    ],
    "require": {
        "php": "^7.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^7",
        "phpstan/phpstan": "^0.10.3"
    },
    "autoload": {
        "psr-4": {
            "Controlabs\\Http\\": "src/Http/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Controlabs\\Tests\\": "tests/"
        }
    },
    "minimum-stability": "stable",
    "scripts": {
        "cs": "phpcs --standard=PSR2 src",
        "test": "phpunit -c phpunit.xml --colors=always",
        "test-cover": "phpunit --coverage-text",
        "phpstan": "phpstan analyse src --level=7"
    }
}
